/* Estilo pag */

* {margin: 0; padding: 0}

:root {
  btnBgColor: #99ffff;
  btnFontColor: #004d4d;
  btnHovColor: #4dffff;
  bgColor: #87CEFA;
  bgListaColor: #FFE4E1;
  bgFecharItem: #ff6666;
  
}

body {
  max-width: 900px;
  margin: 2rem auto;
}

.wapp {
  display: flex;
  flex-direction: column;
  max-width: 100%;
  padding: 1rem;
  border-radius: 10px 0;
	
}

.fontePadrao {
  font-family: 'Calibri', arial;
  font-size: 1rem;
	
}

.main {
  padding: 1rem;
  color: white;
  text-transform: uppercase;
  background-color: var(--bgColor);
  border-redius: 1rem;
	
}


h1 {
  font-size: 1.4rem;
  text-align: center;
  color: var(--btnFotColor);
  margin-bottom: 1rem;
	
}

.incluirAfazer {
   display: flex;
   justify-content: center;
   align-items: center;
	
}

/*Estilos da lista*/

.lista {
  position: relative;
  box-sizing: border-box;
  font-family: 'Arial Black', Arial;
  list-style: nome;
  display: flex;
  align-item: center;
  padding: 0.5rem;
  margin: 5rem;
  max-width: 100%;
  transition: ease backgroud-color 0.6s;
  background-color: var(--bgListaColor);
	
}


.lista:hover {
  transform: background-color;
  box-shadow: 2px 2px 5px;
  background-color: var(--bnrHovColor);
	
}

/*Estilo dos Botões*/

.btnEstiloPadrao {
  height: 3rem;
  min-width: 5rem;
  margin: 0 0.5rem;
  text-transform: uppercase;
  font-weight: bold;
  color: var(--btnFontColor);
  background-color: var(--btnBgColor);
  border-style: nome;
  
}

#novaAtividade {
  padding: 5px auto;

}

#btnLimpar:hover, #novaAtividade:hover {
  background-color: var(--btnHovColor);

}

#descricaoAtividade {
  font-size: 1.1rem;
  padding: 0.5rem;
  height: 3rem

}








